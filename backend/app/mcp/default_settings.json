{
  "server.name": "Manager AI",
  "tool.get_issue_details.description": "Get all details of a specific issue. Returns: id, project_id, name, description, status, priority, specification (markdown), plan (markdown), recap (markdown), tasks (atomic plan tasks), created_at, updated_at.\n\nIssue state machine: NEW → REASONING → PLANNED → ACCEPTED → FINISHED (or CANCELED from any state).",
  "tool.get_issue_status.description": "Get the current status of an issue.\n\nIssue state machine: NEW → REASONING → PLANNED → ACCEPTED → FINISHED (or CANCELED from any state).",
  "tool.get_project_context.description": "Get project information (name, path, description, tech_stack).",
  "tool.set_issue_name.description": "Set the name of an issue after analysis. Can be called at any point to give the issue a meaningful title.",
  "tool.complete_issue.description": "Mark an issue as Finished and save the recap. Only works for issues in Accepted status. All tasks must be completed first. Call this ONLY when the user explicitly asks you to complete the issue.\n\nThis is the final step in the workflow: create_issue_spec → create_issue_plan → create_plan_tasks → (user accepts plan) → (user starts implementation) → (implement & update tasks) → (user asks to complete) → complete_issue.",
  "tool.create_issue.description": "Create a new issue in a project. Parameters: project_id (required), description (required, non-empty), priority (optional int 1-5, default 3). Returns the created issue {id, project_id, description, priority, status='New'}. Use this to autonomously file work items. The issue enters the pipeline in New status — use create_issue_spec next to move it into Reasoning.",
  "tool.create_issue_spec.description": "Write a specification for an issue and move it to Reasoning status. Only works for issues in New status.\n\nThis is the first step in the issue workflow. After writing the spec, use create_issue_plan to write the implementation plan.",
  "tool.edit_issue_spec.description": "Update the specification of an issue in Reasoning status. Use this to refine the spec before creating a plan.",
  "tool.create_issue_plan.description": "Write an implementation plan for an issue and move it to Planned status. Only works for issues in Reasoning status.\n\nAfter creating the plan, use create_plan_tasks to define atomic tasks, then the user can accept_issue to begin implementation.",
  "tool.edit_issue_plan.description": "Update the implementation plan of an issue in Planned status. Use this to incorporate feedback before the plan is accepted.",
  "tool.accept_issue.description": "Accept an issue's plan and move it to Accepted status. Only works for issues in Planned status. Call this ONLY when the user explicitly asks you to accept the plan.\n\nAfter acceptance, wait for the user to tell you to begin implementation. Do NOT automatically start implementing or call start_pipeline — the user controls when work begins.",
  "tool.cancel_issue.description": "Cancel an issue from any status. Use this when the issue is no longer needed or was created in error.",
  "tool.create_plan_tasks.description": "Create atomic tasks for an issue's plan. Accepts a list of task objects with 'name' field. Tasks track implementation progress.\n\nCall this after create_issue_plan to break the plan into trackable steps.",
  "tool.replace_plan_tasks.description": "Replace all tasks for an issue. Deletes existing tasks and creates new ones from the provided list. Use this when the plan changes significantly.",
  "tool.update_task_status.description": "Update the status of an atomic plan task. Valid transitions: Pending → In Progress → Completed.",
  "tool.update_task_name.description": "Update the name of an atomic plan task.",
  "tool.delete_task.description": "Delete a single atomic plan task.",
  "tool.get_plan_tasks.description": "Get all atomic plan tasks for an issue, ordered by execution order.",
  "tool.send_notification.description": "Send a notification to the user via the Manager AI web interface. Use this to notify the user when:\n- You need their input or a decision (e.g., plan is ready for review)\n- A significant milestone is reached (e.g., all tasks completed)\n- An error or blocker requires their attention\n\nDo NOT use this for routine status updates — update task statuses instead so the user can track progress in the UI.",
  "terminal_soft_limit": "5",
  "tool.update_project_context.description": "Update the project context (description and/or tech_stack). Only updates fields that are provided.",
  "work_queue_paused": "false",
  "tool.get_next_issue.description": "Get the next workable issue for a project, ordered by priority. Returns an ACCEPTED issue (ready for implementation) first, then a NEW issue (needs planning). Returns null if no workable issues exist. Only call this when the user explicitly asks for the next issue — do NOT use it to automatically chain work.",
  "terminal_theme": "catppuccin",
  "ask_brainstorm_command": "claude --dangerously-skip-permissions \"/ask-and-brainstorm $project_id\"",
  "claude.skip_permissions": "false",
  "tool.memory_create.description": "Create a memory (title + description, optional parent_id) scoped to a project. Memories are long-term notes the LLM manages across sessions. Use this to record decisions, patterns, constraints, and user preferences.",
  "tool.memory_update.description": "Update a memory's title, description, or parent_id. Passing a new parent_id moves the memory; cycles are rejected.",
  "tool.memory_delete.description": "Delete a memory. Cascades links; children's parent_id becomes null.",
  "tool.memory_get.description": "Fetch a memory with its parent, children, outgoing and incoming links.",
  "tool.memory_list.description": "List memories for a project. Optional parent_id filters to one branch (use empty string for root-level memories).",
  "tool.memory_link.description": "Create a lateral link between two memories (same project). Optional relation label (e.g. 'see_also', 'contradicts').",
  "tool.memory_unlink.description": "Remove a lateral link (matches from_id + to_id + relation).",
  "tool.memory_get_related.description": "Get parent, children, and both directions of lateral links for a memory.",
  "tool.memory_search.description": "Full-text search across a project's memory titles and descriptions (SQLite FTS5). Returns matches with snippet and rank.",
  "tool.list_project_files.description": "List all files uploaded by the user to a project. Returns id, original_name, file_type, file_size, extraction_status (pending/ok/failed/unsupported), low_text flag, created_at. Use this first to discover attached materials (specs, PDFs, spreadsheets) before searching or reading them.",
  "tool.read_project_file.description": "Read the extracted text content of an uploaded file. Supports pagination via offset/max_chars (default 50000, hard cap 500000). Returns {id, name, type, status, content, offset, total_chars, truncated, error}. Only files with extraction_status='ok' contain text. Legacy .doc/.xls are 'unsupported' — ask user to re-upload as .docx/.xlsx.",
  "tool.search_project_files.description": "Full-text search (SQLite FTS5) across filenames and extracted content of uploaded project files. Returns matches with snippet and rank. Use this when you know keywords but not which file contains them. Pattern: list_project_files → search_project_files → read_project_file.",
  "tts.enabled": "false",
  "tts.cap_chars": "500",
  "tts.voice": "",
  "tts.rate": "1.0",
  "tts.pitch": "1.0",
  "tts.summarize_enabled": "false",
  "tts.summarize_model": "claude-haiku-4-5-20251001",
  "tts.summarize_prompt": "Riassumi il seguente testo per la lettura vocale in italiano. Scrivi in prosa scorrevole, massimo {max_length} parole. Niente code block, niente liste puntate, niente markdown. Se ci sono comandi o nomi di file leggili come testo normale.",
  "tts.summarize_max_length": "60",
  "tts.summarize_timeout_seconds": "10",
  "tool.get_project_url.description": "Get the web application URL for a project (for Playwright browser testing). Returns null if project has no URL configured.",
  "tool.list_credentials.description": "List available credential roles for a project (e.g. 'admin', 'user'). Only returns role names, never the actual credential values.",
  "tool.get_credential.description": "Get decrypted credentials for a specific role. Returns url, username, password, and any extra fields. Credentials are decrypted in-memory and never logged.",
  "tool.set_credential.description": "Save or update credentials for a project role. Fields should be a JSON object like {\"username\": \"...\", \"password\": \"...\"}. Encrypted at rest with Fernet.",
  "tool.delete_credential.description": "Delete credentials for a specific role from a project.",
  "tool.list_plugins.description": "List all MCP plugins configured for a project. Returns plugin name, status (running/stopped/error), transport type, access_level, health, and tool count.",
  "tool.get_plugin_config.description": "Get the full configuration of a specific plugin (no secrets). Returns name, transport, command/url, args, env var names (not values), access_level, timeout.",
  "tool.enable_plugin.description": "Enable a plugin and start it. Writes enabled=true to plugins.yaml and spawns the plugin process. Returns success status.",
  "tool.disable_plugin.description": "Disable a plugin and stop it. Writes enabled=false to plugins.yaml and terminates the plugin process. Returns success status.",
  "tool.get_project_links.description": "Get all directional links for a project (both outgoing and incoming). Each link has source_project_id, target_project_id, source_project_name, target_project_name, and description explaining how they relate. Use this to discover which projects might be impacted by changes.",
  "tool.list_agents.description": "List all agents for a project. Returns id, name, role_key, system_prompt, enabled, created_at, updated_at.",
  "tool.create_agent.description": "Create a new agent for a project. Parameters: project_id (required), name (required), role_key (required), system_prompt (optional, default empty). Returns the created agent.",
  "tool.update_agent.description": "Update an existing agent. Parameters: agent_id (required), name (optional), system_prompt (optional), enabled (optional). Only updates fields that are provided.",
  "tool.delete_agent.description": "Delete an agent by ID.",
  "tool.list_pipelines.description": "List all pipelines for a project. Returns id, name, steps (JSON array of {agent_id, order}), is_default, trigger_type.",
  "tool.create_pipeline.description": "Create a new pipeline for a project. Parameters: project_id (required), name (required), steps (list of {agent_id, order}), is_default (optional), trigger_type (optional, default 'issue_accepted').",
  "tool.update_pipeline.description": "Update an existing pipeline. Parameters: pipeline_id (required), name (optional), steps (optional), is_default (optional), trigger_type (optional).",
  "tool.delete_pipeline.description": "Delete a pipeline by ID.",
  "tool.send_agent_message.description": "Send a message to the agent chat for an issue. Parameters: issue_id (required), content (required), message_type (optional, one of: context, decision, question, answer, status). The agent name and role are auto-detected from the calling context.",
  "tool.get_agent_messages.description": "Get all agent chat messages for an issue, ordered by creation time. Use this to read context from other agents before making decisions.",
  "tool.complete_agent_step.description": "Signal that the current agent has completed its work. Parameters: pipeline_run_id (required), summary (required). Marks the running step as completed so the orchestrator can proceed to the next agent.",
  "tool.get_pipeline_status.description": "Get the current status of a pipeline run, including all step statuses. Use this to check pipeline progress.",
  "tool.start_pipeline.description": "Start a pipeline for an issue. Parameters: issue_id (required). Finds the default pipeline and begins execution. Returns the created pipeline run. ONLY call this when the user explicitly asks you to start the pipeline — never start it automatically.",
  "tool.ask_user_question.description": "Ask the user a question through the Manager AI interface. Use this EVERY time you need user input — never use inline text questions or AskUserQuestion. The question appears in the Issue page and the global Questions page. The user can pick an option or write a custom answer. This tool blocks until the user answers or the timeout expires. Parameters: issue_id (required), question (required), options (optional list of strings), timeout_seconds (optional int, default 300)."
}
