---
id: b86a5b74-c991-4c6a-86b4-423002ddf48e
project_id: 1baae1c7-22f1-4091-abec-b49da70cf46c
title: 'Analysis: code paths and fix options'
parent_id: 6d8e5fda-9101-444e-b030-84fda61a324c
created_at: '2026-06-03T12:27:07.625680'
updated_at: '2026-06-03T12:27:13.459300'
links: []
---
COMPLETE ANALYSIS:

1. CODE PATH (ask_user_question):
   MCP server.py:884 → QuestionService.create() → session.add(Question) + session.commit()
   → INSERT INTO questions (DB) → FK constraint issue_id → issues.id

   Meanwhile, issues flow:
   IssueService.create() → issue_store.create_issue() → writes IssueRecord dataclass to file system (.manager_ai/issues/)
   → NEVER touches DB issues table.

2. THE FK:
   Migration 04f837ab5823_add_questions_table.py:33
   ForeignKeyConstraint(['issue_id'], ['issues.id'])
   This was correct when issues were DB-backed, but broke when issues migrated to file-store.

3. VERIFICATION:
   IssueService.get_by_id() (line 110) reads from issue_store.load_issue() — file-backed.
   QuestionService.create() (line 73) does session.add() + session.commit() — DB-backed.
   The tools that create MCP issues (create_issue in server.py:186) use issue_store, not DB.

4. RECOMMENDED FIX:
   Option A (preferred): Create new Alembic migration dropping the FK constraint on questions.issue_id.
   Keep project_id FK since projects DO exist in DB.
   No code changes needed in Question model (it has no SQL-side FK, just a column string).
   
   Option B: Before inserting Question, also INSERT a stub Issue row in DB. More invasive, data sync risk.

5. TEST CONFIRMATION:
   The error was reproduced at runtime — ask_user_question tool returned:
   sqlite3.IntegrityError FOREIGN KEY constraint failed
   INSERT INTO questions (id, project_id, issue_id, ...)