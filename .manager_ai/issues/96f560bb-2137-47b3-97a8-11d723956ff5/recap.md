Fixed Fernet key persistence issue: `_get_fernet()` now raises `ValueError` if `MANAGER_AI_SECRET_KEY` not set (no silent `Fernet.generate_key()` fallback). Added startup key persistence in `main.py` lifespan — reads `data/secret.key` if exists, otherwise generates and writes atomically. Updated `.env.example` with documentation. All existing credentials encrypted with a previously lost key remain unrecoverable.