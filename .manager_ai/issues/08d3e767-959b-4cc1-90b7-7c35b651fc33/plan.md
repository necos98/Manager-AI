## Plan

1. **Add missing import**: In `frontend/src/routes/settings.tsx`, add `useUpdateSetting` to the existing import from `@/features/settings/hooks` on line 5.

That's it — a single atomic change.
