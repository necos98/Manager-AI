MCP plugins:

attualmente ci sono gli mcp plugins per Manager AI, ma sembra che l'utente deve inserire questi plugin. Io voglio che siano sviluppati da parte degli sviluppatori, poi aggiunti e l'utente finale deve solo configurarli in termini di opzioni che richiede il plugin. Il resto è tutto built in, l'utente deve solo abilitarli. Capito?