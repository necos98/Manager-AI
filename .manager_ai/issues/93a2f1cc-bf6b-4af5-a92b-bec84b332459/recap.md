Changed `QuestionCard` to render question text through the existing `MarkdownViewer` component (which wraps `react-markdown` with prose styling) instead of plain-text `<CardTitle>`. One-line change: replaced `<CardTitle className="text-base">{question.question}</CardTitle>` with `<MarkdownViewer content={question.question} />`. No new deps, no backend changes. TypeScript compiles clean.