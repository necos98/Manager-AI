Credo ci sia un problema quando si avvia una pipeline dalla issue. In pratica nel terminale viene inserito "/run-pipeline $issue_id" e quando il modello fetcha le informazioni della issue e cerca la pipeline attiva per quella issue, ritorna null. La funzione "get_active_agent" deve fetchare sulla issue con $issue_id fornito e ritornare la pipeline attiva, perchè io utente ho prima selezionato la pipeline e poi ho cliccato "Run Pipeline" quindi da qualche parte nella issue ci deve essere il riferimento. Ti chiedo di analizza molto scrupolosamente tutti questi aspetti e dirmi se c'è un bug logico o manca proprio l'integrazione 