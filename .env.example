# DATABASE_URL=sqlite+aiosqlite:///data/manager_ai.db  (default, no config needed)
# BACKEND_PORT=8000
# MANAGER_AI_BASE_URL=http://localhost:8000  (used by terminals/hooks to reach the backend; Docker overrides to http://manager-ai.docker:8000)
